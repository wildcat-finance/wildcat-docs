# CLAUDE.md

Agent guidance for this repository lives in [AGENTS.md](AGENTS.md).

For Wildcat protocol knowledge, load the skill at `skills/wildcat-protocol/SKILL.md` — it covers concepts and terminology, borrower/lender workflows, contract internals, and security/legal pointers, with a topic→page routing table.

Quick orientation: Wildcat is a fixed-rate, **undercollateralised** lending protocol (V2; Ethereum mainnet + Plasma). Docs are published via GitBook at https://docs.wildcat.finance — append `.md` to any page URL for clean Markdown, or use `?ask=<question>` for natural-language queries. The curated index is `llms.txt`.
